<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiAPP.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiAPP"
    xmlns:pages="clr-namespace:MauiAPP.Pages"
    Shell.FlyoutBehavior="Flyout"
    Title="MauiAPP">

    <!-- Elemento para la vista de Registro -->
    <FlyoutItem Title="Registro" Icon="icon_registro.png">
        <ShellContent ContentTemplate="{DataTemplate pages:RegistroPage}" />
    </FlyoutItem>

    <!-- Elemento para el verificador de par/impar -->
    <FlyoutItem Title="Verificador Par/Impar" Icon="icon_parimpar.png">
        <ShellContent ContentTemplate="{DataTemplate pages:ParImparPage}" />
    </FlyoutItem>

    <!-- Elemento para el cálculo de edad -->
    <FlyoutItem Title="Cálculo de Edad" Icon="icon_edad.png">
        <ShellContent ContentTemplate="{DataTemplate pages:CalculoEdadPage}" />
    </FlyoutItem>

    <!-- Elemento para la calculadora básica -->
    <FlyoutItem Title="Calculadora Básica" Icon="icon_calculadora.png">
        <ShellContent ContentTemplate="{DataTemplate pages:CalculadoraPage}" />
    </FlyoutItem>

    <!-- Elemento para el conversor de temperatura -->
    <FlyoutItem Title="Conversor Temperatura" Icon="icon_temperatura.png">
        <ShellContent ContentTemplate="{DataTemplate pages:ConversorTemperaturaPage}" />
    </FlyoutItem>

</Shell>
